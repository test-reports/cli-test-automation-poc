[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    smoke: run only the 5 smoke tests (quick sanity checks).
    regression: run all tests (full suite).
